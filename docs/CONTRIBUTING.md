# Legacy Contributing Notes

This file used to contain Streamlit-era contribution instructions. It is kept only as a compatibility pointer for old links.

Current contribution guidance lives in the repository root:

- `CONTRIBUTING.md` for human developer workflow.
- `AGENTS.md` for AI agent workflow and guardrails.
- `tests/README_TESTING.md` for test commands and marker policy.
- `docs/demo-mode-spec.md` for public Demo behavior and workflow.

Do not use old `research_agent/app` or Streamlit commands for new work unless a future migration task explicitly restores that path.
